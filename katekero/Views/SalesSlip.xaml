<UserControl x:Class="katekero.Views.SalesSlip"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <Grid Margin="10" Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="50"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="30"/>
            <RowDefinition Height="10"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition />
                <ColumnDefinition Width="100"/>
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Column="1" 
                       HorizontalAlignment="Center"
                       FontSize="20"
                       Text="納品書"/>
            <StackPanel Grid.Column="2"
                        HorizontalAlignment="Right">
                <TextBlock Text="2025/12/31"/>
                <TextBlock Text="{Binding SaleNo}"/>

            </StackPanel>
        </Grid>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition Width="20"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" 
                        Orientation="Vertical">
                <TextBlock Text="〒998-0102"/>
                <TextBlock Text="山形県酒田市京田２丁目６９−２"/>
                <TextBlock Text="{Binding CustomerName}"/>
            </StackPanel>
            <StackPanel Grid.Column="2" 
                        Orientation="Vertical">
                <TextBlock Text="〒990-8668"/>
                <TextBlock Text="山形県山形市流通センター１丁目"/>
                <TextBlock Text="オビサン株式会社"/>
            </StackPanel>

        </Grid>

        <DataGrid Grid.Row="4"
                      ItemsSource="{Binding Sales}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sale No" Binding="{Binding SaleNo}" Width="*"/>
                <DataGridTextColumn Header="Customer Name" Binding="{Binding CustomerName}" Width="*"/>
                <DataGridTextColumn Header="Product Name" Binding="{Binding ProductName}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
                <DataGridTextColumn Header="Amount" Binding="{Binding Amount}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Grid.Row="6">
            <StackPanel HorizontalAlignment="Right"
                        Orientation="Vertical">
                <TextBlock Text="小計"/>
                <TextBlock Text="消費税"/>
                <TextBlock Text="合計"/>
            </StackPanel>
        </Grid>

    </Grid>
</UserControl>
